#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

if [[ ! -x node_modules/.bin/astro ]]; then
	printf 'Dependencies are missing; rerun .agents/setup.\n' >&2
	exit 1
fi

printf 'Orb dependencies ready.\n'
